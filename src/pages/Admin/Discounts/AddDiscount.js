const AddDiscount = () => {
    return (
      <div>
        <h2 className="text-2xl font-semibold mb-4">Add New Discount</h2>
        <p>Form to create a new product (title, description, image, etc.).</p>
      </div>
    );
  };
  
  export default AddDiscount;